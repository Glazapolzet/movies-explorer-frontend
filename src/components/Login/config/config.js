import { authLinks } from 'shared/config';

export const title = 'Рады видеть!';

export const authButtonText = 'Войти';

export const caption = {
  text: 'Ещё не зарегистрированы?',
  link: 'Регистрация',
  href: authLinks.registration.href,
}
